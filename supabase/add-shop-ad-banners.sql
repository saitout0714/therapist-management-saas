-- ============================================================================
-- 店舗ごとの広告バナー（エステラブ等の相互リンクバナー）を追加
--
-- 作成日 : 2026-08-20
-- 適用先 : PRODUCTION DATABASE
-- 内容   : shops.ad_banners を追加（非破壊）。
--          [{ "imageUrl": "...", "linkUrl": "...", "alt": "..." }, ...] の配列。
--          公開HPのTOPページ末尾（フッター直前）に表示し、HPコンテンツ管理から
--          追加・削除できる。
-- ============================================================================

ALTER TABLE shops ADD COLUMN IF NOT EXISTS ad_banners jsonb DEFAULT '[]'::jsonb;

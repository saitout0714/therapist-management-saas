-- ============================================================================
-- ルームのHP公開用名称を追加
--
-- 作成日 : 2026-08-06
-- 適用先 : PRODUCTION DATABASE
-- 内容   : rooms.public_name を追加（非破壊）。
--          既存の address_nearby / google_map_url_nearby と合わせて、
--          公開HPのアクセスページには「あいまいな公開情報」のみを表示し、
--          実際の部屋番号・詳細住所・内部メモが漏れないようにする。
-- ============================================================================

ALTER TABLE rooms ADD COLUMN IF NOT EXISTS public_name text;
